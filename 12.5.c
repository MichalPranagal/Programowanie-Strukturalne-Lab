#include <stdio.h>
#include <string.h>

int main() {
    printf("%d\n", strlen("Biblioteka") + 1
                 + strlen("string.h") + 1
                 + strlen("pozwala") + 1
                 + strlen("na") + 1
                 + strlen("zastosowanie") + 1
                 + strlen("funkcji") + 1
                 + strlen("do") + 1
                 + strlen("lancuchow") + 1
                 + strlen("znakowych"));
}